{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "http://example.com/example.json",
  "type": "object",
  "title": "The comment schema",
  "description": "The comment schema comprises the entire JSON document.",
  "default": {},
  "examples": [
    {
      "id": 1,
      "text": "test",
      "postId": 2,
      "userId": 1
    }
  ],
  "required": ["text", "postId", "userId"],
  "properties": {
    "id": {
      "$id": "#/properties/id",
      "type": "integer",
      "title": "The id schema",
      "description": "An explanation about the purpose of this instance.",
      "default": 0,
      "examples": [1]
    },
    "text": {
      "$id": "#/properties/text",
      "type": "string",
      "title": "The text schema",
      "description": "An explanation about the purpose of this instance.",
      "default": "",
      "minLength": 1,
      "maxLength": 1000,
      "examples": ["test"]
    },
    "postId": {
      "$id": "#/properties/postId",
      "type": "integer",
      "title": "The postId schema",
      "description": "An explanation about the purpose of this instance.",
      "default": 0,
      "examples": [1]
    },
    "userId": {
      "$id": "#/properties/userId",
      "type": "integer",
      "title": "The userId schema",
      "description": "An explanation about the purpose of this instance.",
      "default": 0,
      "examples": [1]
    }
  },
  "additionalProperties": true
}
